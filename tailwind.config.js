import colors from "tailwindcss/colors";

export default {
    theme: {
        extend: {
            colors: {
                primary: colors.orange,
                secondary: colors.blue,
            },
        },
    },
};
